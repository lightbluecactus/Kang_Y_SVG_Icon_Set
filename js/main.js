(() => {

  console.log('JS is linked!');

    // please refer to js code in each SVG image

    // let theIcon = document.querySelector(".icon");
    // let theIcons = document.querySelectorAll(".icon");

    // function logMyId() {
    // 	  console.log('clicked on an image!');
    // 	  console.log(this.id);
    // }

    // theIcon.addEventListener("click", logMyId);
    // theIcons.forEach(icon => icon.addEventListener("click", logMyId));

  })();